[defaults]
# use local hosts file in this folder
inventory = ./hosts
# Don't worry about RSA fingerprint verification
host_key_checking = False
# Do not create retry files
retry_files_enabled = False
